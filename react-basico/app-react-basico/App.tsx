import Home from "./views/Home";

export default function App() {

  /**
   * no react native, nós desenhamos o layout
   * por meio de componentes, utilizando JSX
   */

  return (
    <Home />
  );
}
