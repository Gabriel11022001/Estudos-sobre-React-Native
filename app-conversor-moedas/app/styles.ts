import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

  container: {
    flex: 1,
    backgroundColor: "#000",
    padding: 20
  },
  containerSelecionarMoeda: {
    width: "100%",
    backgroundColor: "#fff",
    borderRadius: 10
  },
  label: {
    color: "#000",
    fontSize: 16,
    marginStart: 10,
    marginTop: 20
  },
  campo: {
    width: "100%",
    marginStart: 10,
    marginEnd: 10,
    marginTop: 10,
    fontSize: 20,
    marginBottom: 20,
    color: "#000"
  }

});

export default styles;