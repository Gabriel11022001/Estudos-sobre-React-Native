import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  titulo: {
    textAlign: "center",
    color: "#000",
    fontWeight: "bold",
    fontSize: 30,
    marginTop: 40,
    marginBottom: 30,
    fontStyle: "italic"
  }
});

export default styles;