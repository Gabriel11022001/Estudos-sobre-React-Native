import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

  tituloTelaLogin: {
    color: "#000",
    fontSize: 30,
    fontWeight: 900,
    marginTop: 40,
    marginBottom: 40
  }

});

export default styles;